import { Box, Typography } from '@mui/material';

export const ViewConfigJsonPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h3">View Config Json</Typography>
    </Box>
  );
};
